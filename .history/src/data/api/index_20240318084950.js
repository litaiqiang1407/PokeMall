const api = "/api";

const dashboard = `${api}/Dashboard.php`;
const orders = `${api}/Orders.php`;
const products = `${api}/Products.php`;
const users = `${api}/Users.php`;

const suggestions = `${api}/SuggestionProducts.php`;
const productDetail = `${api}/ProductDetail.php`;
const size = `${api}/Size.php`;

export { dashboard, orders, products, users, suggestions, productDetail, size };
