function AddItem() {
  return (
    <div className={cx("add-item")}>
      <div className={cx("header")}>
        <div className={cx("header-title")}>
          <span>Add Item</span>
        </div>
      </div>
    </div>
  );
}

export default AddItem;
